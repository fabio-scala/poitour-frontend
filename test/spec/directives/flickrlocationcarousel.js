'use strict';

describe('Directive: ptFlickrLocationCarousel', function() {

    // load the directive's module
    beforeEach(module('poitourApp'));

    var element, scope;

    beforeEach(inject(function($rootScope) {
        scope = $rootScope.$new();
    }));
    
    // This is tested with E2E tests
});
